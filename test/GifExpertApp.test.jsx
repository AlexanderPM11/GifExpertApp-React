
import { render, renderHook, screen, waitFor } from '@testing-library/react';
import { GifExpertApp } from "../src/GifExpertApp";
describe('Test in Hook <GifExpertApp />', () => {

    test('Debe regresar el estado inicial', () => {
       
        render(   <GifExpertApp /> );
    //    screen.debug()
    });



});
